import { Text, View } from "react-native";
import React from "react";

const SaveBookScreen = ({ navigation, appContext }) => {
  return (<View><Text style={{ color: "black" }}>Save Book Screen</Text></View>);
};
export default SaveBookScreen;
